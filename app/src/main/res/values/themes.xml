<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base Material3 para la app Ecos -->
    <style name="Theme.Ecos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores Ecos (solo los que existen en colors.xml) -->
        <item name="colorPrimary">@color/ecos_primary</item>
        <item name="colorPrimaryDark">@color/ecos_primary_dark</item>
        <item name="colorAccent">@color/ecos_accent</item>
    </style>
</resources>
